// Realtime player updates were previously powered by Supabase Realtime.
// The app now uses the Railway API — player state refreshes on action or page load.
export function useRealtimePlayer(_walletAddress: string | undefined) {}
